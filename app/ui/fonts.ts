import {Montserrat, Roboto, Lusitana} from "next/font/google";

export const montserrat = Montserrat({
    subsets: ['latin'],
    weight: [],
});

export const roboto = Roboto({
    subsets: ['latin'],
    weight: [],
});

export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: ['400', '700'],
});
